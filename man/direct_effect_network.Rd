% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{direct_effect_network}
\alias{direct_effect_network}
\title{Construct a direct-effect network}
\usage{
direct_effect_network(comparisons, anchors = NULL, effect_measure = "HR")
}
\arguments{
\item{comparisons}{A data frame with one row per estimated
drug-versus-drug effect. Required columns: \code{study_id}, \code{target},
\code{comparator}, \code{estimate}, \code{std_error}. Additional metadata columns
(e.g. \code{database}, \code{population}, \code{design}, \code{outcome}) are preserved.}

\item{anchors}{Optional data frame of absolute (placebo-controlled)
estimates. Required columns: \code{study_id}, \code{drug}, \code{reference}
(normally \code{"placebo"}), \code{estimate}, \code{std_error}. Additional columns
are preserved.}

\item{effect_measure}{A single string naming the effect measure the
estimates are on, e.g. \code{"HR"}, \code{"RR"}, or \code{"OR"}.}
}
\value{
An object of class \code{directeffect_network} with components
\code{comparisons}, \code{anchors}, \code{treatments}, \code{graph} (an igraph graph),
\code{components} (integer component membership named by treatment),
\code{effect_measure}, and \code{metadata}.
}
\description{
Builds a validated evidence network from a table of comparative
(drug-versus-drug) effect estimates, optionally alongside a table of
absolute (placebo-anchored) estimates. The returned object defines the
evidence network — it contains no fitted model. Estimates for
multiplicative effect measures (HR, RR, OR) must be supplied on the log
scale.
}
\examples{
comparisons <- data.frame(
  study_id   = c("S1", "S2", "S3"),
  target     = c("A", "A", "B"),
  comparator = c("B", "C", "C"),
  estimate   = c(log(1.02), log(1.34), log(1.29)),
  std_error  = c(0.07, 0.09, 0.08)
)
anchors <- data.frame(
  study_id  = "RCT1",
  drug      = "C",
  reference = "placebo",
  estimate  = log(1.20),
  std_error = 0.04
)
de <- direct_effect_network(comparisons, anchors = anchors,
                            effect_measure = "HR")
de
}
\seealso{
\link{directeffect_formats} for the explicit input and output
format reference, including validation rules.
}
