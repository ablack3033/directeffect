% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-surface.R
\name{fit_surface}
\alias{fit_surface}
\title{Fit the relative direct-effect surface}
\usage{
fit_surface(de, engine = c("netmeta", "stan"), reference = NULL, ...)
}
\arguments{
\item{de}{A \code{directeffect_network} created by
\code{\link[=direct_effect_network]{direct_effect_network()}}. Must be a single connected component; fit
components separately (see \code{\link[=check_connectivity]{check_connectivity()}}) otherwise.}

\item{engine}{\code{"netmeta"} (frequentist network meta-analysis) or
\code{"stan"} (Bayesian, identical likelihood). Both engines fit the same
common-effect model \code{y_k ~ N(theta_target - theta_comparator, se_k^2)} and return the same fit contract.}

\item{reference}{Drug fixed at 0 for identification. Defaults to the
first treatment alphabetically. The choice is arbitrary and does not
affect any estimated difference between drugs.}

\item{...}{Engine-specific options. The Stan engine accepts \code{chains}
(default 4), \code{iter} (default 2000), \code{seed}, \code{refresh} (default 0),
and any further argument to \code{rstan::sampling()}. The netmeta engine
accepts none.}
}
\value{
An object of class \code{directeffect_fit} with components
\code{effects} (tidy per-drug table: \code{drug}, \code{estimate}, \code{std_error},
\code{lower}, \code{upper}, \code{scale}, \code{reference}, \code{engine}), \code{comparisons},
\code{anchors}, \code{heterogeneity}, \code{diagnostics}, \code{engine}, \code{engine_fit}
(the raw engine object — the only place engine internals appear),
and \code{network}.
}
\description{
Estimates the relative positions of all drugs in a connected
direct-effect network from the comparative estimates alone. Anchors
are deliberately ignored at this stage: the surface answers "are my
comparative estimates internally coherent, and what relative structure
do they imply?" — not where that structure sits in absolute terms.
Identification uses an arbitrary reference drug fixed at 0; use
\code{anchor_surface()} to position the surface absolutely.
}
\examples{
comparisons <- data.frame(
  study_id   = c("S1", "S2", "S3"),
  target     = c("A", "A", "B"),
  comparator = c("B", "C", "C"),
  estimate   = c(log(1.02), log(1.34), log(1.29)),
  std_error  = c(0.07, 0.09, 0.08)
)
de <- direct_effect_network(comparisons, effect_measure = "HR")
if (requireNamespace("netmeta", quietly = TRUE)) {
  fit <- fit_surface(de, engine = "netmeta")
  fit$effects
}
}
\seealso{
\link{directeffect_formats} for the explicit schema of every
column in the effects table and the other fit components.
}
