% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectivity.R
\name{check_connectivity}
\alias{check_connectivity}
\title{Report per-component identifiability of a direct-effect network}
\usage{
check_connectivity(de)
}
\arguments{
\item{de}{A \code{directeffect_network} created by
\code{\link[=direct_effect_network]{direct_effect_network()}}.}
}
\value{
A data frame of class \code{directeffect_connectivity} with one row
per connected component and columns \code{component}, \code{n_drugs},
\code{n_comparisons}, \code{n_anchors}, and \code{absolute_identifiable}. The
\code{drugs} attribute holds the drug names per component. Printing
renders the identifiability report.
}
\description{
Comparative estimates identify only differences within a connected
component; a component with no absolute anchor supports relative
effects only, and the package never implies such a component can be
positioned absolutely. \code{check_connectivity()} reports, for each
connected component, the drug count, comparison count, anchor count,
and whether absolute effects are identifiable there.
}
\examples{
comparisons <- data.frame(
  study_id   = c("S1", "S2", "S3"),
  target     = c("A", "A", "B"),
  comparator = c("B", "C", "C"),
  estimate   = c(log(1.02), log(1.34), log(1.29)),
  std_error  = c(0.07, 0.09, 0.08)
)
de <- direct_effect_network(comparisons, effect_measure = "HR")
check_connectivity(de)
}
