% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{validate_recovery}
\alias{validate_recovery}
\title{Measure how well a fit recovers simulation truth}
\usage{
validate_recovery(fit, simulation)
}
\arguments{
\item{fit}{A \code{directeffect_fit} from \code{\link[=fit_surface]{fit_surface()}}, fitted to
\code{simulation$network}.}

\item{simulation}{The result of \code{\link[=simulate_direct_effect_network]{simulate_direct_effect_network()}}.}
}
\value{
A list with \code{bias} (mean error), \code{rmse}, \code{coverage}
(proportion of intervals containing the true value),
\code{rank_correlation} (Spearman correlation of estimated and true
effect ordering), \code{n_drugs} (drugs contributing to the summaries),
and \code{reference}.
}
\description{
Compares a fitted surface against the known true effects of the
simulation that generated its data. Works from the fit contract only,
so it treats every engine identically. Because a surface fit is
relative, the truth is re-centred at the fit's reference drug before
comparison; the reference row itself (estimated as exactly 0 by
construction) is excluded from bias, RMSE, and coverage.
}
\examples{
simulation <- simulate_direct_effect_network(
  n_drugs = 5, n_comparisons = 12, n_anchors = 0,
  heterogeneity = 0, seed = 1
)
if (requireNamespace("netmeta", quietly = TRUE)) {
  fit <- fit_surface(simulation$network, engine = "netmeta")
  validate_recovery(fit, simulation)
}
}
