% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_direct_effect_network}
\alias{simulate_direct_effect_network}
\title{Simulate a direct-effect network with known truth}
\usage{
simulate_direct_effect_network(
  n_drugs = 20,
  n_comparisons = 100,
  n_anchors = 3,
  heterogeneity = 0.1,
  seed = 1,
  effect_sd = 0.5,
  se_range = c(0.05, 0.15),
  anchor_se_range = c(0.03, 0.1)
)
}
\arguments{
\item{n_drugs}{Number of drugs.}

\item{n_comparisons}{Number of comparative estimates; at least
\code{n_drugs - 1} so the network can be connected.}

\item{n_anchors}{Number of placebo anchors, each on a distinct drug
(at most \code{n_drugs}).}

\item{heterogeneity}{Between-study standard deviation \code{tau} added to
every comparison's sampling variance. 0 matches the v0.1
common-effect model exactly.}

\item{seed}{Seed for reproducibility. The global random-number state
is restored afterwards.}

\item{effect_sd}{Standard deviation of the true direct effects around
0 on the log scale.}

\item{se_range}{Range the comparison standard errors are drawn from,
uniformly.}

\item{anchor_se_range}{Range the anchor standard errors are drawn
from, uniformly.}
}
\value{
A list with components \code{network} (a ready-to-fit
\code{directeffect_network}), \code{comparisons}, \code{anchors}, \code{truth} (data
frame of \code{drug}, \code{theta}, with the placebo row at 0), \code{tau}, and
\code{seed}.
}
\description{
Generates true direct effects, a connected comparison graph, standard
errors, observed comparative estimates, and placebo anchor estimates.
The returned object retains the truth so recovery can be measured
rather than assumed — see \code{\link[=validate_recovery]{validate_recovery()}}.
}
\details{
The generative model matches the package's fitting model: observed
comparisons are \code{y ~ N(theta_target - theta_comparator, se^2 + tau^2)}
where \code{tau} is \code{heterogeneity} (0 gives exact model match), and
anchors are \code{a ~ N(theta_drug, se^2)} against \code{reference = "placebo"}
at 0. The comparison graph is connected by construction: a random
spanning tree first, then the remaining comparisons between random
drug pairs (repeat comparisons of the same pair are allowed, as in
real evidence networks).
}
\examples{
simulation <- simulate_direct_effect_network(
  n_drugs = 5, n_comparisons = 10, n_anchors = 1,
  heterogeneity = 0, seed = 1
)
simulation$truth
}
